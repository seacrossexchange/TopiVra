# 🔌 端口标准化配置完成

**配置时间**: 2026-03-12

---

## ✅ 新端口方案（专业标准）

| 服务 | 开发环境 | 生产环境 | 说明 |
|------|---------|---------|------|
| **前端** | 3000 | 80/443 (Nginx) | React 标准端口 |
| **后端** | 8000 | 8000 (内部) | API 标准端口 |
| **MySQL** | 3306 | 3306 (内部) | 数据库标准端口 |
| **Redis** | 6379 | 6379 (内部) | 缓存标准端口 |
| **Nginx** | 80 | 80/443 | Web 标准端口 |

---

## 📝 已更新的文件

### 1. 前端配置
- ✅ `client/vite.config.ts` - 端口 5174 → 3000
- ✅ `client/.env.example` - API URL 更新为 8000

### 2. 后端配置
- ✅ `server/.env` - 端口 3001 → 8000
- ✅ `server/.env.example` - 端口和回调 URL 更新

### 3. Docker 配置
- ✅ `config/docker-compose.yml` - 开发环境端口映射
  - server: 3001:3001 → 8000:8000
  - client: 5174:5174 → 3000:3000
- ✅ `config/docker-compose.prod.yml` - 生产环境端口

### 4. Nginx 配置
- ✅ `config/nginx/dev.nginx.conf` - 上游服务器端口
- ✅ `config/nginx/prod.nginx.conf` - 上游服务器端口

### 5. 文档
- ✅ `README.md` - 访问地址更新
- ✅ `DEPLOYMENT.md` - 部署文档端口更新

---

## 🚀 访问地址（开发环境）

```
前端：http://localhost:3000
后端：http://localhost:8000
API 文档：http://localhost:8000/api/v1/docs
Nginx 代理：http://localhost
MySQL：localhost:3306
Redis：localhost:6379
```

---

## 🎯 优势

### 1. 行业标准
- **3000** - React/Node.js 开发标准端口
- **8000** - RESTful API 常用端口
- **3306** - MySQL 默认端口
- **6379** - Redis 默认端口

### 2. 易于记忆
- 前端：3000（三千）
- 后端：8000（八千）
- 数据库：标准端口

### 3. 避免冲突
- 不使用 5173/5174（Vite 默认）
- 不使用 3001（非标准）
- 使用行业公认端口

### 4. 专业形象
- 符合开发者习惯
- 便于团队协作
- 易于文档说明

---

## 📋 迁移检查清单

- [x] 前端开发端口：3000
- [x] 后端开发端口：8000
- [x] Docker Compose 端口映射
- [x] Nginx 上游服务器配置
- [x] 环境变量文件
- [x] 文档更新
- [x] 回调 URL 更新

---

## 🔄 如何使用新端口

### 开发环境启动

```bash
# Windows
scripts/deploy/START-DEV-WINDOWS.bat

# 访问
前端：http://localhost:3000
后端：http://localhost:8000
```

### 生产环境部署

```bash
# 部署
bash scripts/deploy/deploy-production.sh

# 访问（通过 Nginx）
https://your-domain.com
```

---

## ⚠️ 注意事项

1. **清理旧容器**
   ```bash
   docker-compose down
   docker-compose up -d
   ```

2. **更新本地环境变量**
   - 检查 `client/.env.local`
   - 检查 `server/.env`

3. **浏览器缓存**
   - 清除浏览器缓存
   - 使用新地址访问

4. **OAuth 回调**
   - 更新 Google OAuth 回调 URL
   - 更新 Telegram Bot 回调 URL

---

## 🎉 完成

端口配置已标准化，符合行业最佳实践。所有服务使用专业标准端口，便于开发、部署和维护。

**配置版本**: v1.0
